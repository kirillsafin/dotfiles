### vim-surround
* `cs"'` - change `"` to `'`
* `cs'<q>` - change `"` to `<q>`
* `cst"`
* `ds"` - remove `"`
* `ysiw]` = sorround WORD with `]`
* `S<p class="important">` in Visual Mode

### emmet

* `<C-y>,`

